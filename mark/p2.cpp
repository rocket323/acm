#include <bits/stdc++.h>
using namespace std;

int t, n, c0, c1, c2, a;

int main() {
    scanf("%d", &t);
    while (t--) {
        scanf("%d", &n);
        cout << (n + 1) * n / 2 << endl;
    }
    return 0;
}

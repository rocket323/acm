#include <algorithm>
#include <cstdio>
#include <cstring>
#include <iostream>
#include <map>
#include <queue>
#include <set>
#include <vector>
using namespace std;
using ll = long long;

const int inf = 0x3f3f3f3f;
const int maxl = 2e5 + 10;

int t, x;

int main() {
    scanf("%d", &t);
    for (int tc = 1; tc <= t; tc++) {
        while (true) {
            printf("00001111\n");
            fflush(stdout);
            scanf("%d", &x);
            if (x <= 0)
                break;
        }
    }
    return 0;
}
#include <stdio.h>
#include <cstring>
#define maxl 200010

int n;
int p[maxl];
char s[10], t[10];

int main()
{
	while(scanf("%d", &n) != EOF)
	{
		for(int i=0; i<n-1; ++i)
		{
			scanf("%s%s", s, t);
			int na = strlen(s), nb = strlen(t);
			
		}
	}

	return 0;
}


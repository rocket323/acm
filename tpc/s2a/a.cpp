#include <bits/stdc++.h>

int t, n;

int main() {
    scanf("%d", &t);
    while (t--) {
        scanf("%d", &n);
        if (n > 0) {
            puts("You are the future of Tencent!");
        } else {
            puts("Good luck and Enjoy TPC!");
        }
    }

    return 0;
}
